[TOC]

------------

# 说明
> 该文件夹下主要用于引入第三方库。由于框架文件太大，以下列表的库不再必须集成到目标项目中。

> 请根据实际使用情况进行引入，对于无需使用的库，可以直接将该文件夹下的相应库目录删除以便于减少发布文件体积。

------------
**注意：**`对于一些库的引入需要依赖于其他的一些库，在使用时请认真查阅具体需使用库的相关使用说明。`

# 库列表具体使用方法参照给自库文件的readme.md]
> 1. 中台viewUI框架的引入封装 `lib/viewui/`
> 2. Editor.md富文本编辑器的引入封装 `lib/mdeditor/`
> 3. Vue引入 `lib/vue/`